---
import type { ImageMetadata } from "astro";
import { Image } from "astro:assets";

const { srcs, alts } = Astro.props;
---

<div class="flex gap-4">
  {
    srcs.map((src: ImageMetadata, index: number) => (
      <Image src={src} alt={alts[index]} class="" />
    ))
  }
</div>
